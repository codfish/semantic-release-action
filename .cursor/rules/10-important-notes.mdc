---
description: Important invariants and environment requirements for this action.
globs: "**/*"
alwaysApply: true
---

## Important Notes

- Uses ES modules (`"type": "module"` in `package.json`).
- Configuration precedence: Action inputs > Config files > Defaults.
- Requires `GITHUB_TOKEN` for GitHub releases.
- Container runs as a single process: `node /action/entrypoint.js`.
- Target Node.js version: v22.18.0 (per `.nvmrc`).

Guidance:
- Maintain ESM imports/exports; avoid CommonJS regressions.
- Do not break precedence or dual-output guarantees when changing configuration handling.

